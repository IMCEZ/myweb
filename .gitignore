# 系统与编辑器
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# 依赖与构建产物（本站零依赖，正常不会出现）
node_modules/
dist/

# 本地临时文件
*.log
*.tmp

# ============================================================================
# admin.html 是「本地创作工具」，不应该随站点发布：
#   - 它的访问口令是纯前端的，任何人查看源码都能读到
#   - 它包含写入本地文件的能力，只适合放在你自己电脑上
# 所以这里把它排除在版本控制之外。本地文件保留在你电脑上照常使用。
# 发布新内容时，只需要提交 assets/data/ 与 assets/js/data-embedded.js。
# （Vercel 请参考 .vercelignore；Netlify 拖拽上传时手动不选这个文件）
# ============================================================================
admin.html
